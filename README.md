# Personal Site Starter (Next.js + Tailwind)

## Quick start
1. `npm i`
2. `npm run dev`
3. Open http://localhost:3000

All styles are in `src/app/globals.css`. Colors are controlled by CSS variables.
